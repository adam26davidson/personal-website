---
"@adam26davidson/char-matrix": patch
---

Improve TableElement: distribute expand column widths evenly instead of dumping remainder on last column, support explicit newlines in cell text, and expand data rows vertically to fill available height when table has non-content height sizing.
